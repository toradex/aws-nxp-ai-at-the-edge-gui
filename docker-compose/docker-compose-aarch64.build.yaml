version: '3.7'
services:
  local-ui:
    build:
      # This is relative to docker_compose directory in the current project
      context: ..
      dockerfile: ./dockerfiles/Dockerfile-arm64v8
    image: torizonextras/pasta-demo-local-ui:latest-arm64v8
